spring:
  cloud:
    stream:
      bindings:
        test-out-0:
          error-handler-definition: myErrorHandler
      kafka:
        bindings:
          test-out-0:
            producer:
              sync: true
        binder:
          brokers: localhost:9092
          configuration:
              retries: 3


